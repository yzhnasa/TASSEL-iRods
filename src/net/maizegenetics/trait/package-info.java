/**
 * Phenotype (future) and Trait (deprecated) currently both define classes for holding trait data.  These are currently being merged and
 * trait will be removed.
 * @deprecated Package comes from the TASSEL4 and it is currently being redesigned as part of TASSEL5 as phenotype
 * @author Peter Bradbury
 */
package net.maizegenetics.trait;